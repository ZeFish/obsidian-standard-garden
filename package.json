{
  "name": "standard-garden",
  "private": true,
  "scripts": {
    "build": "node build.js && obsidian vault=Atelier plugin:reload id=standard-garden",
    "dev": "pnpm run build && obsidian dev:console",
    "screenshot": "obsidian dev:screenshot"
  },
  "dependencies": {
    "@stnd/styles": "^0.5.1",
    "@stnd/themes": "^0.5.1",
    "prismjs": "^1.30.0"
  },
  "devDependencies": {
    "esbuild": "^0.28.1"
  }
}
