{
  "name": "standard-garden",
  "private": true,
  "scripts": {
    "build": "node build.js && (command -v obsidian >/dev/null && obsidian vault=Atelier plugin:reload id=standard-garden || true)",
    "dev": "pnpm --filter @stnd/themes run build:themes && pnpm run build && (command -v obsidian >/dev/null && obsidian dev:console || true)",
    "screenshot": "obsidian dev:screenshot"
  },
  "dependencies": {
    "@stnd/styles": "0.10.2",
    "@stnd/themes": "0.8.1",
    "prismjs": "1.30.0"
  },
  "devDependencies": {
    "esbuild": "0.28.1",
    "sass": "1.101.0",
    "yaml": "2.9.0"
  }
}
