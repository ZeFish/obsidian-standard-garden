{
  "name": "standard-garden",
  "private": true,
  "scripts": {
    "build": "node build.js && (command -v obsidian >/dev/null && obsidian vault=Atelier plugin:reload id=standard-garden || true)",
    "dev": "pnpm run build && (command -v obsidian >/dev/null && obsidian dev:console || true)",
    "screenshot": "obsidian dev:screenshot"
  },
  "dependencies": {
    "@stnd/styles": "workspace:*",
    "@stnd/themes": "workspace:*",
    "prismjs": "^1.30.0"
  },
  "devDependencies": {
    "esbuild": "^0.28.1",
    "js-yaml": "^4.3.0",
    "sass": "^1.101.0"
  }
}
